function lookingForSmallScaleCompany() {
    // location.replace(host + '/registration-for-small-scale-company');
    location.replace(host + '/registration');
}

function lookingForMediumScaleCompany() {
    location.replace(host + '/registration-for-medium-scale-company');
}

function lookingForLargeScaleCompany() {
    location.replace(host + '/registration-for-large-scale-company');
}

function tradersRegistration() {
    location.replace(host + '/traders-registration');
}
